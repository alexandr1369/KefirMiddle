namespace DialogueSystem
{
    // public class Dialogues : RuntimeAssets<Dialogue>
    // {
    //     public override void ValidateAssets()
    //     {
    //         if (HaveRedundantAssetsDuplication())
    //             Debug.LogError(this + "\nRedundant assets duplication! Only first asset will be selected.");
    //     }
    //     
    //     private bool HaveRedundantAssetsDuplication() => 
    //         Assets != null && Assets.Any(asset => Assets.FindAll(t => t.Asset.GetType() == asset.Asset.GetType()).Count > 1);
    // }
}